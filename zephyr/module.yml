# Shape derived from carrefinho/prospector-zmk-module (MIT) — see NOTICE.

name: zmk-focus-dongle
build:
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
  depends:
    - lvgl
