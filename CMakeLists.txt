# Nothing to build yet. The focus-block timer core lands in phase 3 and the
# shield in phase 2; this file exists so `build: cmake: .` in zephyr/module.yml
# resolves and the module can be loaded by a ZMK build.
